# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build and dependencies
node_modules
dist
dist-ssr
*.local

# Firebase settings (avoid exposing secrets)
.env
.env.local
.env.development
.env.production
.env.test
.firebaserc  # Ignore only if not required in the repo
firebase.json  # Ignore only if not required in the repo
src/utils/firebase.ts  # Ignore Firebase config file to prevent leaking credentials

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
